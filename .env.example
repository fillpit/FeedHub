# FeedHub 环境变量配置示例

# 服务器端口
PORT=8009

# 运行环境
NODE_ENV=development

# API基础URL
API_BASE_URL=http://localhost:8009

# 前端API基础URL代理
VITE_API_BASE_URL_PROXY=http://127.0.0.1:8009

# 基础认证用户名和密码
BASIC_AUTH_USERNAME=admin
BASIC_AUTH_PASSWORD=admin@123

# JWT密钥
JWT_SECRET=your-jwt-secret-key

# Redis连接URL（可选）
REDIS_URL=redis://localhost:6379

# 脚本存放目录（可选）
# 如果不设置，默认使用项目根目录下的 scripts 目录
# 可以设置为绝对路径或相对于项目根目录的相对路径
# SCRIPTS_DIR=/path/to/custom/scripts
# SCRIPTS_DIR=custom-scripts

# 自定义包存放目录（可选）
# 如果不设置，默认使用项目根目录下的 custom_packages 目录
# 可以设置为绝对路径或相对于项目根目录的相对路径
# CUSTOM_PACKAGES_DIR=/path/to/custom/packages
# CUSTOM_PACKAGES_DIR=custom-packages

# Telegram频道配置（可选）
# TELE_CHANNELS=channel1,channel2

# 书籍存储路径（可选）
# 如果不设置，默认使用项目根目录下的 uploads/books 目录
# 可以设置为绝对路径或相对于项目根目录的相对路径
# BOOKS_STORAGE_PATH=/path/to/books/storage
# BOOKS_STORAGE_PATH=custom-books-storage